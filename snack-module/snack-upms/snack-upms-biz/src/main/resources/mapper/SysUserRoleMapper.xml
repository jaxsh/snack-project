<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jax.snack.upms.biz.mapper.SysUserRoleMapper">

    <select id="selectEnabledRoleCodesByUsername" resultType="java.lang.String">
        SELECT ur.role_code
        FROM sys_user_role ur
                 JOIN sys_role role ON ur.role_code = role.role_code AND role.status = #{status}
        WHERE ur.username = #{username}
    </select>

</mapper>
